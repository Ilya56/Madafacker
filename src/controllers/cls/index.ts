/**
 * This is utility to store data in the cls. It uses a nest-cls solution
 * This approach is used to avoid user object providing in each function and to not use Scope.REQUEST, that
 * is not recommended by Nest.js
 */

export * from './cls.module';
export * from './cls.data';
