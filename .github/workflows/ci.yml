name: Node.js CI v2

on:
  pull_request:
    branches:
      - develop
  push:
    branches:
      - main
      - develop

jobs:
  build-and-test:
    uses: ./.github/workflows/build-and-test.yml
    with:
      node-version: '20.x'
